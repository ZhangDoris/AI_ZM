# 2026-06-09 工作日志

## 更新：项目后台管理 — 二三级菜单结构

按用户截图重构了「项目后台管理」左侧菜单：

### 菜单结构
- **项目管理**（二级，直接菜单项，原有）
- **系统管理**（二级，可展开分组）
  - 用户管理（三级，原有 buildAdminUsers）
  - 角色管理（三级，占位页）
  - 字典管理（三级，占位页）
  - 疾病领域字典管理（三级，占位页）
  - 医院管理（三级，新增实质内容页 buildAdminHospitals）
  - 审批流配置（三级，占位页）
  - 系统设置（三级，占位页）
  - 系统文案设置（三级，占位页）
  - 菜单管理（三级，新增树形表格页 buildAdminMenus）
- **匹配管理**（二级，新增 buildAdminMatching，含统计卡片+配置表格）
- **接口调用记录**（二级，新增 buildAdminApiLog，含调用日志表格）

### 技术实现
- 新增CSS类：`.sidebar-group`、`.sidebar-group-header`、`.sidebar-group-body`、`.sidebar-group-arrow`、`.sidebar-item-l3`
- 新增JS：`adminGroupState` 展开状态对象、`toggleAdminGroup(key)` 切换展开收起
- 重写 `buildAdminSidebar()` 为分组嵌套结构
- 新增通用占位页 `buildAdminPlaceholder(icon, title, desc)`
- 新增内容页：`buildAdminHospitals()`、`buildAdminMenus()`、`buildAdminMatching()`、`buildAdminApiLog()`
- 折叠状态下分组子菜单自动收起（`max-height:0`），仅显示分组图标

## 更新：项目管理列表功能重构

按用户提供的真实截图（txt文件），重写 `buildAdminProjects()` 函数：

### 搜索表单（10个字段，3列网格布局）
项目编号、方案编号、项目名称、项目阶段、领域、申办方、适应症、试验分期、负责人、项目状态

### 工具栏
左：新增项目、同步人员信息、同步项目信息  
右：列设置（浮层弹窗）、导入、导出

### 表格列（共17列+操作，支持列设置显示/隐藏）
序号、项目编号、方案编号、项目名称、项目阶段（badge）、申办方、适应症、试验分期、项目经理、医学经理、市场经理、项目状态（上架状态badge）、领域、创建人、创建时间、更新人、更新时间、操作（编辑/下架/删除）

### 数据
10条真实数据（来自用户系统截图），共123条分页

### 新增CSS
`.form-row-item`（表单行容器）、`.form-lbl`（表单标签）

### 新增JS
`toggleProjColPanel()`（列设置浮层开关）  
`window._projColConfig`（列显示状态持久化）

## 更新：操作列按钮美化 + 固定列 + 文字截断

- 操作列从文字链接改为圆角边框按钮（进入项目/编辑/下架/删除，不同颜色）
- 已上架项目显示「进入项目」蓝色按钮
- 项目编号列（含复选框、序号）固定在左侧，操作列固定在右侧，中间可横向滚动
- 固定列带阴影分界线
- 文字长列默认截断6字符+…，hover显示完整内容（title属性）

## 更新：顶部菜单重构 — 项目管理提升为一级菜单

- 新增顶部一级菜单「🗂️ 项目管理」（位于「智能筛选」之后、「数据统计」之前）
- 「项目后台管理」改名为「后台管理」
- 「后台管理」侧边栏移除「项目管理」子菜单（已提升为一级菜单）
- 点击「项目管理」直接展示项目部列表页（无侧边栏）
- 所有面包屑导航中的「项目后台管理」同步改为「后台管理」
- 项目管理页面面包屑简化为「当前位置：项目管理」

## 更新：新增AI改进分析模块

### 一级菜单
新增顶部一级菜单「🤖 AI改进分析」（位于「项目中心邀请码」之后、「后台管理」之前），含左侧二级菜单：
- **AI改进分析设置** — 模型输入字段配置（14字段）、定时执行（每周五22:00）、手动执行（按项目）、执行日志
- **AI改进分析报告** — 统计概览卡片、项目报告列表（可点击查看详情）
- **改进意见管理** — 流程图展示 + 医学经理反馈意见列表

### 参考文件
SHR-1918-302_改进分析报告.html — 报告详情的结构和数据参照源

### 技术实现
- 新增CSS：`.ai-settings-grid`、`.ai-field-tag`、`.ai-exec-status`、`.ai-report-card`、`.ai-log-row` 等
- 新增JS函数：`buildAIImproveSidebar()`、`buildAIImproveContent()`、`buildAISettings()`、`buildAIReports()`、`buildAIReportDetail()`、`buildAIFeedback()`、`runManualAnalysis()` 等
- switchModule 新增 `aiimprove` 分支
- 顶部导航新增 `nav-aiimprove` 菜单项

## Bug修复：登录按钮无法点击

**问题**：`buildAIReportDetail()` 中改进方案列表的嵌套三元运算符有两个多余的引号：
```js
// 错误（行4546-4547）
const prioColor = imp.priority==='P0'?'#fff3cd':'imp.priority==='P1'?'#fef7e0':'';
const prioBadge = imp.priority==='P0'?'badge-red':'imp.priority==='P1'?'badge-yellow':'badge-blue';
// 修复：去掉第二个条件前的多余单引号

## 更新：AI改进分析设置优化

### 模型输入字段美化
- 从简单标签改为卡片网格布局（`grid-template-columns: repeat(auto-fill, minmax(200px, 1fr))`）
- 每个字段显示：序号、中文名称、字段key、必填/可选徽章
- 必填字段左侧红色边框，可选绿色边框
- hover 时有上浮和蓝色阴影效果

### 定时执行时间可编辑
- 星期改为下拉选择（周一至周日）
- 时间改为 `<input type="time">` 控件
- 新增 `updateScheduleDay(day)` 和 `updateScheduleTime(time)` 函数
- 修改后自动刷新 nextRun 显示
- 开关改为 Ant Design 风格的 toggle 滑块

### 执行日志独立成页
- 新增侧边菜单「📋 执行日志」
- `buildAILogs()` 函数：统计卡片（总次数/成功/定时/手动）+ 完整日志表格
- 支持按类型过滤（全部/定时/手动）、刷新按钮
- `renderAILogs()` 独立渲染函数
- `switchModule` 中 aiimprove/logs 分支调用 renderAILogs

### 手动执行支持全部项目
- 下拉框新增「📦 全部项目」选项（`value="__ALL__"`）
- `runManualAnalysis()` 根据选项动态计算项目数、病例数和耗时
- 全部项目执行时间随机3-6分钟，单个项目20-60秒

## 更新：执行日志重构

- 移除统计卡片区域
- 新增查询条件栏：时间范围（date start/end）、类型（全部/定时/手动）、状态（全部/成功/失败）
- 新增按钮：🔍 查询、↺ 重置、🗑 一键清空（带确认弹窗）
- `renderAILogs()` 支持全部筛选条件组合过滤
- 底部显示「共 N 条记录」
- 修复执行策略文案：「拒绝」→「拒绝」、「已排除」或「已有批注」

## 更新：AI改进分析报告页面重构

- 新增查询条件栏：项目编号、方案编号、项目名称（三输入框）
- 新增按钮：🔍 查询、↺ 重置
- 移除 4 卡片统计概览，替换为一句话统计摘要条：
  共 X 个项目 | 平均AI准确率 X% | 最高准确率项目数量 X | 最低准确率项目数量 X
- `renderAIReports()`：按条件过滤并更新统计数字和列表
- `resetAIReportFilters()`：清空条件并刷新
- `renderAIReportList(projects)`：提取为独立函数，支持动态渲染
- 数据中新增 `planNo` 字段（方案编号），卡片中显示方案编号行
- 新增 CSS：`.ai-report-summary-bar`

## 更新：全局移除 page-header

- 移除所有页面中的 `page-header` 块（标题+描述），面包屑已能指示当前位置
- 对于含 `header-actions` 的页面，将操作按钮保留并转为紧凑的 flex 操作栏
- 涉及页面：智能筛选、数据概览、趋势分析、邀请码、用户权限管理、入排标准配置、医院管理、菜单管理、匹配管理、接口调用记录、脱敏管理、测试后台、AI设置、执行日志、AI报告、报告详情、改进意见管理、后台管理占位页
- 报告详情页保留精简标题行 + 蓝色底边，去掉冗余描述
const prioColor = imp.priority==='P0'?'#fff3cd':imp.priority==='P1'?'#fef7e0':'';
const prioBadge = imp.priority==='P0'?'badge-red':imp.priority==='P1'?'badge-yellow':'badge-blue';
```
由于 JS 语法错误导致整个 `<script>` 块解析失败，`doLogin()` 等所有函数都无法执行。

## 更新：AI改进分析报告 — 排序功能 + 列表美化

- **排序功能**：在查询条件栏中新增排序字段下拉（AI准确率 / 实际通过率）和排序方向按钮（↓降序 / ↑升序），可自由切换
- 排序按钮为 toggle 样式，蓝色高亮激活态
- 统计摘要栏右侧显示当前排序提示（如"按AI准确率 降序排列"）
- `toggleReportSortOrder()` 函数：切换排序方向并刷新列表
- `renderAIReports()` 中集成排序逻辑，对过滤后的列表按选中字段和方向排序

- **列表美化**：卡片式布局改为表格式列表布局
  - `ai-report-table` 容器 + 表头行（灰色背景）
  - 数据行使用 CSS Grid 10列布局，斑马纹交替背景
  - hover 浅蓝高亮，可点击跳转详情
  - 列：项目编号（蓝色加粗）、方案编号、项目名称（截断+title）、病例数、🤖AI准确率、📊实际通过率、⚠️问题、💡建议、状态（圆点+文字）、箭头

- 新增 CSS：`.ai-sort-bar`、`.ai-sort-select`、`.ai-sort-order-btn`、`.ai-report-table` 系列
